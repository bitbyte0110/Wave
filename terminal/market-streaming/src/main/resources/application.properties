# Server (runs on 8081 for market data & STOMP ws)
server.port=8081

# Redis Cache (Docker port 6379)
spring.data.redis.host=localhost
spring.data.redis.port=6379

# No MySQL, no RabbitMQ, no Security/JWT, no Gemini key needed
